{
  "name": "@ldesign/doc-playground",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm exec ldoc dev",
    "build": "pnpm exec ldoc build",
    "preview": "pnpm exec ldoc preview"
  },
  "devDependencies": {
    "@ldesign/doc": "workspace:*"
  }
}
