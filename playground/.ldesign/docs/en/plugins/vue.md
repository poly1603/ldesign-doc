# Vue Plugin

`vuePlugin` enhances Vue-related Markdown fences for demo scenarios.

## Installation

```ts
import { vuePlugin } from '@ldesign/doc/plugins'

export default defineConfig({
  plugins: [
    vuePlugin({
      devtools: true
    })
  ]
})
```
